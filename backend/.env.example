PORT=5000
# MySQL — replace user/password with your local MySQL credentials
DATABASE_URL="mysql://root:Tiko88tiko@localhost:3306/tort_bakery"


# Legacy — remove after Stage 4 (Mongoose → Prisma migration)
MONGODB_URI=mongodb://localhost:27017/tort-bakery

# Telegram notifications (optional)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Email notifications (optional — wire up nodemailer later)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
NOTIFY_EMAIL=
