PORT=5000
# MySQL — replace user/password with your local MySQL credentials
DATABASE_URL="mysql://root:password@localhost:3306/tort_bakery"

# Telegram notifications (optional)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Email notifications (optional — wire up nodemailer later)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
NOTIFY_EMAIL=
